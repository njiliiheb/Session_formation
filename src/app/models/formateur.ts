export interface Formateur {
  id: string;
  nom: string;
  prenom: string;
  email: string;
  telephone: string;
  cin: string;
  photo: string; // URL simulée
  cv: string; // URL simulée
  specialites: string[];
}
